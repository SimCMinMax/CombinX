# SimCMinMax CVars
$(class)=rogue
$(sim_type)=default
$(root)=Player
$(dev_state)=html

# SimC Global CVars (Comment if you want to override in CLI, or put them after global call)
threads=8
process_priority=normal
target_error=0.1
optimal_raid=1
max_time=330
vary_combat_length=0.3
fight_style=void

# Init chaining variables
input=$(chain_path)init.simc
